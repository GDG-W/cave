import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class ContainerProperties {
  static var defaultRadius = Radius.circular(8.0.r);
  static var defaultBorderRadius = BorderRadius.all(defaultRadius);

  static var defaultSecondaryRadius = Radius.circular(4.0.r);
  static var defaultSecondaryBorderRadius =
      BorderRadius.all(defaultSecondaryRadius);

  static var defaultPrimaryRadius = Radius.circular(56.0.r);
  static var defaultPrimaryBorderRadius =
      BorderRadius.all(defaultPrimaryRadius);
}
