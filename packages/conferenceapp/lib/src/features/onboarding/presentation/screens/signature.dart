import 'package:flutter/material.dart';

class OnboardingSignatureScreen extends StatelessWidget {
  const OnboardingSignatureScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
